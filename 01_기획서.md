# YouTube 투자정보 수집 앱 기획서

## 목적

유튜브 채널의 투자 콘텐츠를 자동 수집하고, LLM으로 기사 형태로 재구성하여 투자 의사결정을 지원하는 로컬 웹 앱.

## 설계 원칙

- LLM 호출은 자막 재구성 1회만. 나머지는 일반 코드 처리
- 웹 앱 + 백그라운드 서비스를 단일 프로세스로 결합
- SQLite 단일 DB (FTS5 전문 검색 포함)
- 앱→오픈클로 Push 방식 동기 호출

## 데이터 파이프라인

```
RSS 폴링(코드) → 자막 추출(코드) → 키워드 필터링(코드) → 기사 변환(LLM) → 저장(코드) → 텔레그램 알림(코드)
```

| 단계 | 처리 | 설명 | 방식 |
|------|------|------|------|
| 1 | RSS 폴링 | 구독 채널 Atom 피드를 주기적으로 조회하여 새 영상 감지 | 코드 |
| 2 | 자막 추출 | 등록/자동생성 자막 추출 (youtube-transcript-api) | 코드 |
| 3 | 필터링 | 키워드 매칭으로 투자 관련성 판단, 무관한 영상 제외 | 코드 |
| 4 | 기사 변환 | 자막을 고정 포맷의 기사 형태로 재구성 (오픈클로 API) | **LLM** |
| 5 | 저장 | 재구성된 문서를 SQLite에 보관 | 코드 |
| 6 | 알림 | 배치 단위로 텔레그램 봇 발송 | 코드 |

## 재구성 문서 포맷

LLM 프롬프트로 생성하는 고정 구조:

- **제목**: 영상 제목을 기사 제목 스타일로 재작성
- **리드**: 핵심 3줄 요약
- **본문**: 주장 – 근거 – 사례 – 결론 구조
- **팩트 박스**: 숫자, 고유명사, 용어 정의
- **원문 링크**: 영상 URL + 주요 타임스탬프

## 로컬 웹 앱 (프론트엔드)

1. 영상 목록 조회: 구독 채널별 영상, 메타데이터(제목, 썸네일, 업로드 시간), 재구성 상태 표시
2. 자막 조회: 원본 자막과 재구성된 기사 열람
3. 구독 관리: 채널 추가/해지
4. 정렬 및 검색: 구독 목록별/업로드 시간별 정렬, 키워드 텍스트 검색
5. 원본 이동: YouTube 원본 링크로 바로 이동

## 백그라운드 서비스

### RSS 폴링 및 영상 감지
- 구독 목록의 채널 RSS 피드를 15~30분 주기로 폴링
- RSS 피드는 최근 15개만 반환하므로 주기가 너무 길면 누락 가능
- 채널 수가 많을 경우 비동기 처리

### 자막 추출 및 분기
- 새 영상의 메타데이터(제목, 업로드 시간, 채널명)와 썸네일 저장
- youtube-transcript-api로 자막 추출
- 자막 없는 영상은 "자막 없음" 상태로 표시, LLM 큐에서 제외
- 향후 Whisper fallback 확장 가능

### LLM 재구성 큐 처리
- DB에 상태 컬럼(pending/processing/done/failed)으로 순차 처리
- 앱이 오픈클로 API를 직접 호출하는 Push 방식
- 오픈클로 API 응답을 동기로 받아 즉시 DB에 저장
- 재시도: 실패 시 지수 백오프로 재시도, 최대 3회 실패 시 "수동 확인 필요" 상태
- 긴 자막은 토큰 한도 초과에 대비한 분할 처리 전략 필요

### 텔레그램 알림
- 한 번의 배치(RSS 폴링 사이클)에서 쌓인 스크립트 처리가 모두 완료되면 알림 발송
- 알림 내용: 리드(3줄 요약) + 제목 + 원본 링크
- 배치 단위로 묶어 발송하여 알림 피로도 방지

## 데이터 저장소

SQLite + 파일시스템. 단일 사용자 로컬 환경에 최적.

| 테이블 | 주요 필드 |
|--------|----------|
| channels | channel_id, channel_name, rss_url, is_active, created_at |
| videos | video_id, channel_id, title, upload_time, thumbnail_path, transcript_status, restructure_status, retry_count, created_at |
| transcripts | video_id, raw_text, language, source_type(manual/auto) |
| articles | video_id, title, lead, body, fact_box, timestamps, created_at |

- FTS5로 자막 및 기사 키워드 검색
- 썸네일은 파일시스템 저장, DB에는 경로만 기록
- 상태 컬럼으로 큐 처리 추적 (pending/processing/done/failed)

## 오픈클로 연동

- 자막 재구성 단계에서만 호출 (Push 방식 동기 호출)
- 프롬프트 템플릿은 앱 측에서 관리
- 고려사항: API 타임아웃 설정, 토큰 한도 초과 시 분할 처리, 지수 백오프 재시도

## 기술 스택

| 영역 | 기술 |
|------|------|
| 언어 | Python 3.11+ |
| 웹 프레임워크 | FastAPI (uvicorn) |
| 프론트엔드 | Jinja2 + HTMX + Tailwind CSS (CDN) |
| 백그라운드 스케줄러 | APScheduler |
| 데이터 저장소 | SQLite (aiosqlite) + FTS5 |
| RSS 폴링 | feedparser + YouTube Atom 피드 |
| 자막 추출 | youtube-transcript-api |
| LLM 재구성 | 오픈클로 API (httpx) |
| 알림 | python-telegram-bot |
| 썸네일 저장 | 로컬 파일시스템 |

**라우트 분리 원칙**: `/api/*`(JSON)과 `/views/*`(HTMX HTML 조각)을 분리하여, 추후 React 전환 시 `/views/*`만 제거하고 `/api/*`는 그대로 유지한다.

## 리스크

| 위험도 | 리스크 | 대응 |
|--------|--------|------|
| 높음 | 비공식 라이브러리(youtube-transcript-api) 의존. YouTube 내부 API 변경 시 중단 | Whisper fallback 예비 |
| 중간 | 자동생성 한국어 자막 오인식 | LLM 프롬프트에 노이즈 전제 설계 |
| 중간 | RSS 최근 15건 제한으로 누락 가능 | 폴링 주기 15~30분 |
| 낮음 | LLM API 비용 누적 | 키워드 필터링으로 불필요 호출 제거 |

## 향후 확장

- Whisper 음성인식 fallback
- 중복/상충 감지 (여러 채널 관점 비교)
- 팩트 신뢰도 태깅
- YouTube Data API v3 병행
