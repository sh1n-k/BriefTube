# BriefTube 기술 검토 종합 결과

- 작성일: 2026-02-24
- 기준 문서:
  - `01_기획서.md`
  - `02_개발스펙.md`

## 1) APScheduler + feedparser 적합성 검토

### 결론
현재 기획(단일 프로세스 로컬 앱, 15~30분 주기 폴링, SQLite 상태 관리)에는 `APScheduler + feedparser` 조합이 적절하다.

### 이유
- 단일 앱 내부에서 주기 작업(RSS poll, retry, notifier) 관리가 쉽다.
- 피드 파싱에서 일반적인 RSS/Atom 예외를 다루기 편하다.
- 초기 MVP 목표(빠른 구현, 낮은 운영 복잡도)와 맞다.

### 주의사항
- 장기적으로 다중 프로세스/다중 인스턴스로 확장할 경우 스케줄/큐 구조 재설계가 필요하다.
- YouTube 피드 누락 방지를 위해 폴링 주기, 워터마크, 중복 방지 로직을 함께 둬야 한다.

### 대안 비교
1. `Celery + beat`
- 장점: 분산 처리/확장성 우수, 대규모 큐 운영에 강함
- 단점: Redis/RabbitMQ 등 운영 복잡도 증가, 현재 MVP에는 과함

2. `RQ + cron`
- 장점: Celery 대비 단순
- 단점: Redis 의존, 워커/스케줄러 분리 운영 필요

3. `ARQ`
- 장점: asyncio 친화, Redis 기반으로 비교적 경량
- 단점: Redis 운영 필요, 장기 유지보수 판단 필요

4. `httpx + ElementTree`(feedparser 대체)
- 장점: 의존성 축소, HTTP 제어 세밀
- 단점: RSS/Atom 예외 처리와 파싱 보정 로직을 직접 유지해야 함

## 2) Python 표준 라이브러리(배터리) 대체 가능성

### 결론
가능은 하지만, 현재 단계에서 완전 대체는 비권장.

### 대체 방식
- 스케줄링: `asyncio` 루프 + `sqlite3` 상태 관리
- 피드 파싱: `urllib`/`http.client` + `xml.etree.ElementTree`
- 날짜 처리: `datetime`, `email.utils`

### APScheduler + feedparser 대비 장단점
1. 장점
- 외부 의존성 감소
- 구현 제어권 증가
- 매우 단순한 요구에서는 충분

2. 단점
- 스케줄 정책(미스파이어, 중복 실행 제어, 재시도)을 직접 구현해야 함
- RSS/Atom 변형 대응, 조건부 요청(ETag/Last-Modified)까지 직접 관리 필요
- 코드량/테스트 부담 증가

### 권장 방향
- MVP는 `APScheduler + feedparser` 유지
- 의존성 최소화가 최우선일 때만 표준 라이브러리 중심으로 전환 검토

## 3) 스펙 문서 기반 개선 대안

### 1. 자막 수집: 단일 의존 → 다중 fallback
- 현재: `youtube-transcript-api` 단일 의존
- 대안: `youtube-transcript-api` + `yt-dlp` + `faster-whisper` fallback
- 이유: 외부 변경 시 전체 파이프라인 중단 위험 완화
- 비용: 구현 복잡도, CPU 사용량 증가

### 2. 상태 컬럼 큐 → `jobs` 테이블 + lease 패턴
- 현재: `videos.restructure_status` 중심
- 대안: `jobs(job_type, status, lease_until, attempt, last_error)` 분리
- 이유: 재시작/장애 복구/중복 실행 방지 강화, 이력 추적 용이
- 비용: 스키마/코드 복잡도 증가

### 3. RSS 고정 폴링 → 조건부 요청 + 워터마크
- 현재: 고정 주기 폴링
- 대안: `ETag`, `Last-Modified`, `last_seen_published_at` 사용
- 이유: 누락 방지 + 트래픽 절감
- 비용: 구현 포인트 증가

### 4. FTS5 생성만 명시 → 동기화 트리거 명시
- 현재: FTS 테이블 생성 중심
- 대안: INSERT/UPDATE/DELETE 트리거 추가
- 이유: 검색 인덱스 불일치 예방
- 비용: 마이그레이션 관리 필요

### 5. JSON TEXT 저장 → JSON1 활용 또는 정규화
- 현재: `fact_box`, `timestamps`를 TEXT(JSON) 저장
- 대안: JSON1 쿼리 전제 명시 또는 별도 정규화 테이블 분리
- 이유: 향후 필터/정렬/통계 확장성 확보
- 비용: 초기 구현 복잡도 증가

### 6. 오프셋 페이지네이션 → 키셋 페이지네이션
- 현재: `page, limit`
- 대안: `cursor(upload_time, video_id)` 방식
- 이유: 대량 데이터에서 성능/일관성 개선
- 비용: API/클라이언트 구현 난이도 소폭 상승

## 4) 적용 우선순위 제안

### 즉시 반영 권장
1. 자막 수집 다중 fallback 전략
2. FTS 동기화 트리거
3. RSS 워터마크/조건부 요청

### 후속 반영 권장
1. `jobs` 테이블 분리
2. 키셋 페이지네이션
3. JSON 정규화

## 5) 최종 판단

- 현재 MVP 방향은 타당하다.
- 다만 운영 안정성 관점에서 위 3개(다중 fallback, FTS 동기화, RSS 조건부 요청)는 초기에 반영하는 편이 총비용을 줄인다.
- 분산 처리 수준의 복잡도(Celery 계열)는 실제 트래픽/채널 수 증가 시점에 단계적으로 도입하는 것이 적절하다.
