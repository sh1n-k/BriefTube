# 기술 스택 검토 보고서

검토일: 2026-02-24

기획서(`01_기획서.md`)와 개발 스펙(`02_개발스펙.md`)에 명시된 기술 스택을 항목별로 검토하고,
더 나은 대안이 있는 경우 변경 근거와 함께 권장안을 제시한다.

---

## 변경 요약

| 구성요소 | 현재 스펙 | 권장 | 판정 |
|----------|----------|------|------|
| 백그라운드 스케줄러 | APScheduler | `asyncio.create_task` + sleep | **교체** |
| RSS 파싱 | feedparser | `httpx` + `xml.etree.ElementTree` | **교체** |
| 텔레그램 알림 | python-telegram-bot | httpx 직접 호출 (Telegram Bot API) | **교체** |
| DB | aiosqlite | aiosqlite (유지) | 유지 |
| 자막 추출 | youtube-transcript-api | 유지 + yt-dlp fallback 추가 | **보강** |
| HTTP 클라이언트 | httpx (async) | httpx (유지) | 유지 |
| 웹 프레임워크 | FastAPI (uvicorn) | FastAPI (유지) | 유지 |
| 프론트엔드 | Jinja2 + HTMX + Tailwind CSS | 유지 | 유지 |
| LLM | 오픈클로 API (httpx) | 유지 | 유지 |

교체 대상 3개의 공통점: httpx가 이미 스택에 포함되어 있어 별도 라이브러리 없이 해결 가능하다.
외부 의존성이 3개 줄어들고, async 일관성이 높아진다.

---

## 1. 백그라운드 스케줄러: APScheduler → asyncio.create_task

### 현황

| 항목 | 내용 |
|------|------|
| APScheduler 안정 버전 | 3.11.2 (2025-12-22) |
| v4 상태 | 4.0.0a6 알파 — 프로덕션 비권장 |
| async 지원 | `AsyncIOScheduler`로 FastAPI 연동 가능하나 래핑 방식 (진정한 async-native 아님) |
| 유지보수 | v3은 유지보수 모드, v4는 완전 재설계 중이나 안정화 시점 불명확 |

### 변경 근거

1. **작업이 단순하다**: RSS 폴링(15분 간격), Transcript Fetcher, LLM Queue Worker, Telegram Notifier — 총 4개 주기/조건 작업. cron 표현식이 필요 없고 interval + DB 상태 트리거만으로 충분하다.
2. **의존성 제거**: APScheduler + pytz 등 의존성 체인이 사라진다.
3. **async-native**: FastAPI와 동일한 이벤트 루프에서 자연스럽게 동작한다.
4. **v3/v4 분열 리스크 회피**: v3은 유지보수 모드, v4는 알파. 어느 쪽을 선택해도 장기적 리스크가 있다.

### 권장 구현 패턴

```python
from contextlib import asynccontextmanager

@asynccontextmanager
async def lifespan(app: FastAPI):
    tasks = [
        asyncio.create_task(rss_poll_loop()),
        asyncio.create_task(transcript_fetch_loop()),
        asyncio.create_task(llm_queue_loop()),
    ]
    yield
    for t in tasks:
        t.cancel()

app = FastAPI(lifespan=lifespan)

async def rss_poll_loop():
    while True:
        try:
            await do_rss_polling()
        except Exception:
            logging.exception("RSS polling failed")
        await asyncio.sleep(900)  # 15분
```

### 앱 중단 후 복원

APScheduler의 영속 job store(`SQLAlchemyJobStore`)를 사용하면 스케줄 자체를 복원할 수 있다.
그러나 이 프로젝트는 **DB 상태 컬럼(`transcript_status`, `restructure_status`)이 실질적인 복원 메커니즘**이므로,
스케줄러가 스케줄을 기억하느냐 여부는 중요하지 않다.

| 시나리오 | asyncio.create_task | APScheduler (영속 스토어) |
|----------|--------------------|-----------------------|
| 스케줄 자체 복원 | X (메모리 소멸) | O |
| 실제 작업 복원 | DB 상태로 복원 | DB 상태로 복원 |
| 실질적 차이 | 없음 | 없음 |

재시작 시 각 워커의 복원:

- **RSS Poller**: 즉시 폴링 → DB에 없는 video_id만 INSERT → 누락 없음
- **Transcript Fetcher**: `transcript_status=pending` 조회 → 자동 재개
- **LLM Queue Worker**: `restructure_status IN (pending, processing)` 조회 → 자동 재개
- **Telegram Notifier**: 완료 건 기준 발송

`processing` 상태에서 크래시한 경우, 앱 시작 시 stuck 상태를 복구한다:

```sql
UPDATE videos SET restructure_status = 'pending'
WHERE restructure_status = 'processing';
```

### 검토한 대안

| 대안 | 장점 | 단점 | 적합도 |
|------|------|------|--------|
| **asyncio.create_task + sleep** | 의존성 0, async-native, 가장 단순 | cron 미지원, 영속화 없음 | **최적** |
| APScheduler 3.x | 성숙, cron/interval, job store 영속화 | v3 유지보수 모드, v4 마이그레이션 불확실 | 양호 |
| Celery + Beat | 업계 표준, 분산, 모니터링 | Redis/RabbitMQ + 별도 프로세스 필요, 과도 | 부적합 |
| arq | async-native, 가벼움 | Redis 필수 | 부적합 |
| Huey | 간단한 API, SQLite 스토어 | async 미지원, 별도 워커 필요 | 부적합 |
| Rocketry | 선언적 문법 | 프로젝트 중단 (12개월+ 릴리스 없음) | 비권장 |
| Taskiq | async-native, FastAPI 통합 | 브로커(Redis 등) 필요, 분산 불필요 시 과도 | 부적합 |
| fastapi-utils `@repeat_every` | 데코레이터 하나로 간단 | 프로젝트 업데이트 빈도 낮음, `on_event` deprecated | 중간 |

---

## 2. RSS 파싱: feedparser → httpx + xml.etree.ElementTree

### 현황

| 항목 | 내용 |
|------|------|
| feedparser 안정 버전 | 6.0.12 (2025-09-10) |
| async 지원 | 없음 (의도적 결정 — HTTP 코드 제거 예정) |
| 유지보수 | 1인 관리, 불규칙 릴리스 (2022→2023→2025) |
| 의존성 | sgmllib3k (11년간 업데이트 없는 라이브러리)에 의존 |

### 변경 근거

1. **YouTube Atom 피드는 구조가 고정적이다**: RSS 0.9x/1.0/2.0/CDF/Atom 0.3 등을 모두 처리하는 feedparser의 "유니버설 파서" 능력이 필요 없다.
2. **피드당 최대 15개 entry**: 데이터량이 적어 무거운 추상화 레이어가 불필요하다.
3. **추가 의존성 0**: `xml.etree.ElementTree`는 Python 표준 라이브러리, httpx는 이미 스택에 포함.
4. **YouTube 전용 필드 처리**: `yt:videoId`, `media:group` 등을 직접 파싱하는 것이 feedparser의 변환된 키(`entry.get('yt_videoid')`)보다 직관적이다.
5. **sgmllib3k 레거시 의존성 제거**: 11년간 업데이트 없는 의존성 체인을 피할 수 있다.

### YouTube Atom 피드 구조

```xml
<feed xmlns:yt="http://www.youtube.com/xml/schemas/2015"
      xmlns:media="http://search.yahoo.com/mrss/"
      xmlns="http://www.w3.org/2005/Atom">
  <entry>
    <yt:videoId>VIDEO_ID</yt:videoId>
    <title>Video Title</title>
    <published>2025-01-15T10:00:00+00:00</published>
    <media:group>
      <media:thumbnail url="https://i2.ytimg.com/vi/VIDEO_ID/hqdefault.jpg"
                       width="480" height="360"/>
      <media:description>Video description...</media:description>
    </media:group>
  </entry>
  <!-- 최대 15개 entry -->
</feed>
```

### 권장 구현 패턴

```python
from xml.etree.ElementTree import fromstring

NAMESPACES = {
    "atom": "http://www.w3.org/2005/Atom",
    "yt": "http://www.youtube.com/xml/schemas/2015",
    "media": "http://search.yahoo.com/mrss/",
}

async def fetch_youtube_feed(client: httpx.AsyncClient, channel_id: str):
    url = f"https://www.youtube.com/feeds/videos.xml?channel_id={channel_id}"
    resp = await client.get(url)
    root = fromstring(resp.text)
    return [
        {
            "video_id": entry.find("yt:videoId", NAMESPACES).text,
            "title": entry.find("atom:title", NAMESPACES).text,
            "published": entry.find("atom:published", NAMESPACES).text,
            "thumbnail": entry.find("media:group/media:thumbnail", NAMESPACES).get("url"),
        }
        for entry in root.findall("atom:entry", NAMESPACES)
    ]
```

### 검토한 대안

| 대안 | 장점 | 단점 | 적합도 |
|------|------|------|--------|
| **httpx + xml.etree.ET** | 의존성 0, async, 빠름, 30줄이면 충분 | 네임스페이스 직접 처리 (일회성) | **최적** |
| httpx + feedparser | 범용 피드 호환, 검증된 파서 | sgmllib3k 레거시, 동기 파싱, 과잉 | 양호 |
| atoma | 타입 힌트, defusedxml 보안 | 5년 이상 업데이트 없음 | 비권장 |
| fastfeedparser (Kagi) | feedparser 대비 25x 빠름 | lxml C빌드 필요, 신생 프로젝트 | 대량 처리 시만 |
| reader | 완전한 피드 리더 시스템 | 목적 자체가 다름, async 미지원 | 부적합 |

---

## 3. 텔레그램 알림: python-telegram-bot → httpx 직접 호출

### 현황

| 항목 | 내용 |
|------|------|
| python-telegram-bot 버전 | v22.6 (2026-01-24) |
| 패키지 크기 | ~7.8MB |
| 이 프로젝트에서 사용하는 기능 | `sendMessage` **1개** |

### 변경 근거

1. **기능의 1% 미만 사용**: Application, Updater, Handler, ConversationHandler 등 봇 프레임워크 전체가 딸려오지만, 이 프로젝트는 "배치 완료 후 sendMessage 호출" 하나만 필요하다.
2. **의존성 0 추가**: httpx가 이미 스택에 포함되어 있다. FastAPI 앱의 `httpx.AsyncClient` 인스턴스를 그대로 재사용하면 커넥션 풀도 공유 가능하다.
3. **Telegram Bot API 안정성**: `sendMessage` 엔드포인트는 수년간 변경 없이 유지되어 왔다. 라이브러리 업데이트 추적의 필요성이 거의 없다.

### 권장 구현 패턴

```python
class TelegramNotifier:
    BASE_URL = "https://api.telegram.org/bot{token}"

    def __init__(self, token: str, chat_id: str, client: httpx.AsyncClient):
        self.url = self.BASE_URL.format(token=token) + "/sendMessage"
        self.chat_id = chat_id
        self.client = client

    async def send(self, text: str, parse_mode: str = "HTML") -> dict:
        resp = await self.client.post(
            self.url,
            json={
                "chat_id": self.chat_id,
                "text": text,
                "parse_mode": parse_mode,
            },
        )
        resp.raise_for_status()
        return resp.json()
```

### 검토한 대안

| 대안 | 추가 의존성 | httpx 재사용 | 적합도 |
|------|-----------|-------------|--------|
| **httpx 직접 호출** | 0 | O | **최적** |
| python-telegram-bot | 1 (~7.8MB) | X (내부 인스턴스) | 과잉 |
| aiogram | 6+ (aiohttp 등) | X (aiohttp 사용) | 과잉 + HTTP 클라이언트 이중화 |
| Pingram | 1 (~20KB) | 불확실 | 월 434회 다운로드, 불안정 |

---

## 4. DB: aiosqlite — 유지

### 현황

| 항목 | 내용 |
|------|------|
| 버전 | 0.22.1 (2025-12-23) |
| 유지보수 | 활발, 주간 105만+ 다운로드 |
| async 방식 | 전용 스레드 1개로 sqlite3를 asyncio에 브릿징 |
| FTS5 호환 | 완벽 — sqlite3 표준 라이브러리의 모든 기능 사용 가능 |

### 유지 근거

1. **FTS5가 핵심 기능**: ORM을 도입해도 FTS5 관련 코드는 전부 raw SQL로 작성해야 한다. ORM이 커버하는 영역(단순 CRUD)과 raw SQL이 필요한 영역(FTS5)이 혼재하면 코드 일관성이 떨어진다.
2. **테이블이 4개뿐**: ORM의 가치는 테이블 수가 많고 관계가 복잡할 때 극대화된다.
3. **의존성 최소화**: aiosqlite 하나면 충분하다.
4. **성능**: `fetchall()` 패턴을 사용하면 동기 sqlite3와 성능 차이가 거의 없다. (`fetchone()` 반복은 최대 15배 느리므로 피할 것)

### 타입 안전성 보완

```python
from pydantic import BaseModel

class Video(BaseModel):
    video_id: str
    channel_id: str
    title: str
    transcript_status: str

async def get_video(db: aiosqlite.Connection, video_id: str) -> Video | None:
    async with db.execute(
        "SELECT video_id, channel_id, title, transcript_status FROM videos WHERE video_id = ?",
        (video_id,)
    ) as cursor:
        row = await cursor.fetchone()
        return Video(**dict(zip([d[0] for d in cursor.description], row))) if row else None
```

### 검토한 대안

| 대안 | 판단 | 이유 |
|------|------|------|
| Tortoise ORM | 부적합 | FTS5 가상 테이블을 모델로 매핑 불가, 4개 테이블에 과잉 |
| SQLModel | 부적합 | 아직 0.x (0.0.34), FTS5 우회 필요, 의존성 스택이 두꺼움 |
| databases (encode) | **사용 불가** | 2025년 8월 아카이브됨 (read-only) |
| sqlite3 + run_in_executor | 가능하나 불필요 | aiosqlite가 이 패턴을 자동화한 것이므로 재발명 |

---

## 5. 자막 추출: youtube-transcript-api — 유지 + fallback 보강

### 현황

| 항목 | 내용 |
|------|------|
| 버전 | v1.2.4 (2026-01-29) |
| 유지보수 | 활발 — 2025년 3월 v1.0.0 이후 1년간 12회 릴리스 |
| Python 지원 | 3.8 ~ 3.14 |
| async 지원 | 없음 — `asyncio.to_thread()` 래핑 필요 |

### v1.0 Breaking Change (주의)

v1.0.0에서 정적 메서드 방식이 인스턴스 메서드로 전환되었다.

```python
# v0.x (더 이상 동작하지 않음)
transcript = YouTubeTranscriptApi.get_transcript("VIDEO_ID")

# v1.0+ (현재 방식)
ytt_api = YouTubeTranscriptApi()
transcript = ytt_api.fetch("VIDEO_ID")
raw_data = transcript.to_raw_data()
```

### 신뢰성

- **로컬 환경에서 IP 차단 위험 낮음**: 클라우드 서버에서는 거의 확실히 차단되지만, 이 프로젝트는 로컬 웹 앱이므로 유리하다.
- **분기 1~2회 YouTube 내부 API 변경 대응 필요**: 메인테이너가 수일~수주 내 대응하나, 1인 개발 프로젝트라는 리스크 존재.
- **쿠키 인증 현재 비활성화**: 연령 제한 영상 접근 불가. 투자 콘텐츠는 대부분 해당 없음.

### async 통합 패턴

```python
ytt_api = YouTubeTranscriptApi()

async def fetch_transcript(video_id: str):
    transcript = await asyncio.to_thread(ytt_api.fetch, video_id)
    return transcript.to_raw_data()
```

### Fallback 체인 권장

기획서의 "Whisper fallback"을 다단계로 보강한다.

```
1차: youtube-transcript-api    (비용 $0, 빠름)
  ↓ 실패 시
2차: yt-dlp 자막 추출            (비용 $0, 영상 다운로드 없이 자막만)
  ↓ 실패 시
3차: 오디오 → gpt-4o-mini-transcribe  ($0.003/분)
```

- **yt-dlp**: 대규모 커뮤니티, 쿠키/OAuth 인증 지원, 연령 제한 영상 접근 가능.
  `yt-dlp --skip-download --write-auto-sub --sub-lang ko --sub-format json3 "VIDEO_URL"`
- **gpt-4o-mini-transcribe**: 기존 Whisper와 동일 가격대에서 더 높은 정확도 (2025년 출시).
  로컬 Whisper 대비 GPU 불필요.

초기 Phase 2에서는 youtube-transcript-api만 구현하고,
`no_subtitle` 영상이 실제로 얼마나 발생하는지 관찰한 후 fallback 필요성을 판단한다.

---

## 6. 변경 후 기술 스택 (권장)

| 영역 | 기술 |
|------|------|
| 언어 | Python 3.11+ |
| 웹 프레임워크 | FastAPI (uvicorn) |
| 프론트엔드 | Jinja2 + HTMX + Tailwind CSS (CDN) |
| 백그라운드 스케줄러 | `asyncio.create_task` + sleep (FastAPI lifespan) |
| 데이터 저장소 | SQLite (aiosqlite) + FTS5 |
| RSS 파싱 | httpx + xml.etree.ElementTree |
| 자막 추출 | youtube-transcript-api (+ yt-dlp fallback 예비) |
| LLM 재구성 | 오픈클로 API (httpx) |
| 알림 | Telegram Bot API (httpx 직접 호출) |
| HTTP 클라이언트 | httpx (async) |
| 썸네일 저장 | 로컬 파일시스템 |

### 변경 후 시스템 구조

```
┌──────────────────────────────────────────────────────┐
│                    단일 프로세스 앱                      │
│                                                      │
│  ┌────────────────────┐  ┌─────────────────────────┐ │
│  │  FastAPI 웹 서버     │  │  asyncio background     │
│  │                    │  │  tasks (lifespan)       │ │
│  │  /api/*   (JSON)   │  │                         │ │
│  │  /views/* (HTMX)   │  │  RSS Poller             │ │
│  │  /*       (Pages)  │  │  Transcript Fetcher     │ │
│  │                    │  │  LLM Queue Worker       │ │
│  │  Jinja2 + HTMX     │  │  Telegram Notifier     │ │
│  └──────────┬─────────┘  └────────────┬────────────┘ │
│             │                         │              │
│             └────────────┬────────────┘              │
│                          │                           │
│                    ┌─────▼─────┐                     │
│                    │  SQLite    │                     │
│                    │  + FTS5    │                     │
│                    └───────────┘                     │
└──────────────────────────────────────────────────────┘
           │                    │
           ▼                    ▼
     ┌───────────┐      ┌─────────────┐
     │ 오픈클로 API │      │ Telegram API │
     └───────────┘      └─────────────┘
```

### 외부 의존성 비교

```
변경 전 (6개):
  fastapi, uvicorn, aiosqlite, httpx,
  apscheduler, feedparser, python-telegram-bot,
  youtube-transcript-api

변경 후 (4개):
  fastapi, uvicorn, aiosqlite, httpx,
  youtube-transcript-api

제거: apscheduler, feedparser, python-telegram-bot (-3개)
```
